import { registerRootComponent } from 'expo';

import App from './App';

// This file is the explicit web entrypoint for Expo Web.
// Expo and Metro will resolve this file when bundling for the web platform.
registerRootComponent(App);
